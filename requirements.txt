# 本项目要求 Python 3.11.x（推荐 3.11.9 稳定版）
# Windows:  py -3.11 -m venv .venv
# Linux:    python3.11 -m venv .venv

# 推理 & 训练（训练所需）
ultralytics>=8.1.0

# 部署端推理（ONNX Runtime CPU）
onnxruntime>=1.17.0
onnx>=1.15.0

# GUI
PyQt5>=5.15.0
PyQt-Fluent-Widgets>=1.11.0

# 图像 IO / 处理
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Excel 报表（无需 MS Office）
openpyxl>=3.1.0

# PDF 报告
reportlab>=4.0.0

# 诊断面板（可选，无 psutil 时仅 RAM 监控失效）
psutil>=5.9.0

# 开发依赖（可选，运行测试时需要）
pytest>=7.4.0
